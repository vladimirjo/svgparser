<!DOCTYPE root [
    <!-- <!ELEMENT root ((a | (b, (c | (d, e*)))), f)> -->
    <!ELEMENT root (a, b*)*>
    <!ELEMENT a EMPTY>
    <!ELEMENT b EMPTY>
    <!ELEMENT c EMPTY>
    <!ELEMENT d EMPTY>
    <!ELEMENT e EMPTY>
]>

<root>
    <a/>
    <c/>
    <a/>
    <a/>
    <a/>
</root>
